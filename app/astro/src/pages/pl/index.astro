---
// PL root redirect: /pl -> /pl/o-mnie
// Use base URL to ensure correct path for GitHub Pages deployment
const baseUrl = import.meta.env.BASE_URL;
const targetUrl = `${baseUrl}/pl/o-mnie`.replace(/\/+/g, '/');
return Astro.redirect(targetUrl, 301);
---
